# Resume-Analyzer
ResumeAnalyzer is a Flask-based web application that automatically parses resumes and analyzes them against job descriptions. It uses NLP libraries like spaCy and NLTK to extract skills and keywords from PDF resumes and calculates how well a candidate's profile matches a given job role.
